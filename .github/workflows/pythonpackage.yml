name: Python package

on:
  push:
  pull_request:
  schedule:
    - cron: '0 6 * * *'  # Daily 6AM UTC build

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: [3.7, 3.8, 3.9, "3.10"]
      fail-fast: false

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        sudo apt -y update
        sudo apt -y install devscripts bzr rustc cargo
        pip install --upgrade pip cython
        pip install -U pip setuptools flake8 mypy pyyaml testtools setuptools-rust
        mkdir $HOME/.config/breezy/plugins -p
        bzr branch lp:brz brz
        make -C brz
        make -C brz install
        bzr branch lp:brz-debian ~/.config/breezy/plugins/debian
        pip install -U git+https://salsa.debian.org/jelmer/debmutate \
                       git+https://salsa.debian.org/jelmer/lintian-brush \
                       "git+https://salsa.debian.org/debian/distro-info#egg=distro-info&subdirectory=python"
        python setup.py develop
    - name: Style checks
      run: |
        python -m flake8
    - name: Typing checks
      run: |
        python -m pip install types-setuptools types-PyYAML types-dataclasses types-chardet
        python -m mypy silver_platter
    - name: Test suite run
      run: |
        python -m unittest silver_platter.tests.test_suite
      env:
        PYTHONHASHSEED: random
