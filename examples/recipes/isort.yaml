# To use this recipe, install isort:
# $ pip install isort
---
name: isort
command: ['sh', '-c', 'isort --om -q .; echo "Sort Python import definitions with isort"']
mode: propose
merge-request:
  commit-message: Sort Python import definitions
  description:
    markdown: Sort Python import definitions
